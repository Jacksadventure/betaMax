# Runtime dependencies for the benchmark runners and utilities in this checkout.
#
# The legacy Python betaMax backend referenced by some scripts is not bundled
# here, so the supported quickstart path uses the C++ backend under betamax_cpp/.

# Used by match.py, which is the fallback oracle for regex benchmarks.
regex>=2024.0.0

# Used by report.py.
matplotlib>=3.8.0

# Used by data_fetch.py.
requests>=2.31.0
